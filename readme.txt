The data for Questionnaire is provided in the .SQL files provided in the sql/ folder.

Run the sql files in your MySql interface.

Bootsrap files are also provided in CSS/ , fonts/ and JS/ folders. 

The upoaded images will be uploaded in the images/ folder.

Some sample images matching images (size,resolution) are provided.